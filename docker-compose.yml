
services:
  mongodb:
    build: ./mongo
    container_name: movie-api-db
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db
      - ./mongo/init-mongo.sh:/docker-entrypoint-initdb.d/init-mongo.sh

volumes:
  mongo-data:
    driver: local